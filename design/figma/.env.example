# Figma REST API personal access token (read access to file content).
# Create at: Figma → Settings → Account → Personal access tokens
# Do not commit a real token; use .env (gitignored) or export FIGMA_TOKEN=...
FIGMA_TOKEN=
